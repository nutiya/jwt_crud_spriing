spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5432/StudentManagement
    username: postgres          # your local postgres username
    password: Raksa-14     # your local postgres password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update          # auto-update schema
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
app:
  jwt:
    secret: MySuperSecretKeyForJWTGeneration12345
    expiration: 15m
    refresh-expiration: 30d